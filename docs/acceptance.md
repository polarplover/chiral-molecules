# 本地验收记录

日期：2026-09-16。测试成绩只在开发浏览器中产生，不是学生作业或同学试玩记录，没有公开提交。

## 自动检查

最终结果：50项通过、0失败（社区35项、章节10项、离线及脚本语法5项）。

实际运行 `node --test community/*.test.mjs tests/*.test.mjs`（即 `npm test` 对应命令；本机运行环境没有 npm 命令）：覆盖社区协议、六题所有错误选项与重答、提示门槛、100/60/30等权计分、刷新恢复、进度边界、私密反思、章节存档隔离、相对路径和原首页接入。离线缓存与内联脚本语法由独立测试检查。

将核实过的原始 `index.html` 重新应用 `tools/integrate-chirality.mjs` 中的限定替换，所得内容与交付文件完全一致；旧章节内容与引擎没有额外修改。上游索引 Git blob SHA：`7c18ee7f4e35644f8187fe29fe7069d4a6fb358f`。

`python tools/restore-upstream-assets.py --verify-only`：477/477通过，缺失或损坏0；包括81张WebP与396个MP3，合计186,756,840字节。逐项核验长度与Git blob SHA，清单保留于 `docs/upstream-tree.json`。

社区测试用隔离夹具验证正确目的地、Issue草稿往返解析、快照校验、同分并列、关帖撤回、HTML作为数据、伪造字段拒绝。测试数据不进入生产 `community.json`。

另外验证配置后的仓库可以处理有数据快照；检查不再要求社区永远为空，避免真实同学提交后阻断Actions。工作流已静态检查触发条件、固定action版本、可信main同步及最小权限；真正的GitHub运行仍待部署后验证。

## 实际浏览器检查

| 检查 | 结果 |
| --- | --- |
| 原项目桌面及390×844窄屏 | 打开argon与mrna，观察逐页、倾向、错误反馈、纠错、线索与阅读器；不代表学生本人通关。 |
| 本地项目路径 | 使用 `/socrates-question/` 而非仅域名根路径；首页新入口可以进入本章，原10关仍显示。 |
| 完整通关 | 14页走到结算；键盘Enter完成题目和八颗晶体分类。 |
| 三类计分 | 首答2、纠错2、提示2，实际结算63分；另一轮首答6题结算100分。 |
| 错误反馈 | 具体解释与当前错误选项对应；第二题逐一点击所有错误选项后仍可重答。 |
| 提示 | 点击后下一页仍不可用，正确作答后才解锁。 |
| 刷新与重置 | 预测页刷新恢复；重开chirality后，argon仍在已建立的第2页书签。 |
| 预测不泄漏 | 第5页和第7页的图、替代文本与当前DOM仅有封存记录；后续结果在推进后创建。源码并非考试保密系统。 |
| 手机 | 390px宽：scrollWidth375；320px宽：scrollWidth305。题页、分类、结算及含长昵称的榜单均无横向溢出。 |
| 空榜 | 0参与者、无真实成绩和评论；未配置仓库时说明清楚。 |
| 有数据榜单 | `tests/community-preview.html` 隔离页面展示明确标注的测试夹具；HTML字符串显示为文本，未产生注入图片/脚本。 |
| 离线 | 停止本地HTTP服务器后刷新成功，并从分类继续至结算；首次访问仍需联网完成缓存。 |
| 控制台 | 正常在线通关后页面error/warn列表为空；开发工具自身的连接提示不计作页面错误。 |
| 不公开私密反思 | 成绩包只有白名单字段；昵称/星级/评论由玩家另行自愿填写。 |

## 尚需真实外部验收

未部署、未运行真实GitHub Actions、未发真实Issue。部署后的权限、Pages HTTPS可达性和一次真实投稿同步，需要用户按部署文档操作后验证。当前 `site-config.js` 故意没有仓库目的地。

7—9分钟是设计估计，需至少三位真实同学试玩检验阅读时间与难度。用户本人仍需真实完成原作两关并填写 `homework-notes.md`。开发测试不能替代这些要求。

## 2026-09-17 配图整合复验

用户提供五图原样重命名，映射至第1、4、5、8、13页；保留科学SVG。全套自动测试51/51通过（新增配图阶段/资源/缓存检查，离线缓存版本更新为v2）。浏览器实际检查桌面和390px手机开场：图片成功加载，scrollWidth375小于视口390，无横向溢出，页面error列表为空。五图均逐张查看过内容；未发现提前展示实验读数。素材来源及命名对照见pics/README.md。旧标签页关闭后更新的Service Worker才接管，首次下载完成后可缓存配图。
